node_modules/
site/*.html
_notused
.claude/settings.local.json
.env

site/video/
site/api/sangupackageversion.php
site/api/twtactics-redirect.exe

# Tracked as deployment inputs for the Docker/Coolify build (no more updates expected):
# sp-config.json (consumed by build.ts) and sangupackage.user.js (served as a download).
